# deploy Quad9 as the system (resolv) resolver?
ubuntu_dns_quad9: false

# deploy default system resolv.conf ?
ubuntu_dns_system: false

# Ubuntu timezone
ubuntu_timezone: America/Los_Angeles

# Install and enable NTP service?
ubuntu_ntp_enable: true